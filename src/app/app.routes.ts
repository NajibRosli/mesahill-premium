import { Routes } from '@angular/router';

export const routes: Routes = [
  // Single page layout with anchor scrolling
  // All components are rendered on the main page
  // Navigation is handled via scrollIntoView
];
